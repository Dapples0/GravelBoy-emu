#ifndef TIMER_H
#define TIMER_H

// #include "MMU.h"

class Timer {
    public:
        Timer();
        ~Timer();

        // void connect(MMU *mmu);
    private:
        // MMU *mmu;
};






#endif